# 安全政策（Security Policy）

## 支持版本

| 版本 | 支持状态 |
|---|---|
| 最新 Release（v0.3.x+） | ✅ 安全修复 |
| 更早版本 | ❌ 请升级 |

## 漏洞报告

- **首选**：GitHub Security Advisories（仓库 Security 标签页 → Report a vulnerability），仅仓库维护者可见；
- 备选：Issue 并标注 `security`（若内容不含可利用细节）。
- 请勿在公开渠道披露未修复的漏洞细节。回复目标：72 小时内确认，修复时间视严重程度另定。

## 安全边界（设计基线，任何补丁不得削弱）

1. **默认仅回环（H-3，CLI/MCP 适用）**：CLI 与 MCP 的所有网络传输默认仅允许 `127.0.0.0/8`；
   URL 校验 + 仅回环模式拨号源地址绑定双重强制，域名解析结果全量断言。公网访问需显式放行：
   `porter-mcp -allow-remote`（仅 MCP 有此参数）或 CLI 显式 `-proxy`
   （设置代理即视为允许出站），打开后即为常规下载器行为（放行模式使用默认源地址拨号）。
   例外：TUI 以人类为第一用户，默认放行公网（第 27 轮用户裁决，`AllowRemote=true`），
   代理仅作可选项。
2. **FTP PASV 反弹防御（RFC 2577）**：被动模式返回地址必须与控制连接对端一致。
3. **重定向策略**：跳数上限 10；重定向协议白名单；跨主机剥离
   `Cookie` / `Authorization` / `Proxy-Authorization`。HLS 跨主机段请求同此策略。
4. **加密协议不做 DRM 绕过**：HLS 仅支持 `AES-128` 明文密钥协商；`SAMPLE-AES` 等
   DRM 相关方法显式拒绝。
5. **资源有界**：播放列表 ≤1MiB / 段数 ≤2048 / 单段 ≤64MiB / Metalink 元文件 ≤1MiB /
   候选 ≤32——全部外部输入有上限，防滥用与放大攻击。
6. **状态文件权限**：任务状态含完整 URL（可能含凭据），目录 `0700`、`state.json` `0600`。
7. **零第三方运行时依赖**（核心模块）：`go list -m all` 仅含本模块，第三方供应链面为零；
   tui/mcp 独立 module 的依赖见 [COMPLIANCE.md](COMPLIANCE.md)。

## 遥测声明

本项目**不含任何遥测、上报、数据收集**。唯一的对外网络行为是把用户提供的 URL
发往对应服务器本身。CI 中仅运行 govulncheck（官方漏洞库比对，不下发构建产物数据）。
